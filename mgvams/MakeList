# MakeList for mgvams
PACKAGE = mgvams

TARGET = \
  mg_filt_xdt.so \
  mg_filt_noise.so \
  mg_func_analysis.so \
  mg_func_math.so \
  mg_func_pf.so \
  mg_func_port.so \
  mg_task_random.so \
  mg_func_sys.so \
  mg_task_write.so

NOTYET_TARGET = \
  mg_task.so

# BUG: not here
MAKE_CPPFLAGS = -I ${top_srcdir}/src

INSTALL_FILES = $(TARGET)
INSTALL_DIR = $(PREFIX)/lib/gnucap/${PACKAGE}

RAW_SRCS = ${TARGET:%.so=%.cc}
SRCS = ${RAW_SRCS}

CLEANFILES = ${TARGET}
