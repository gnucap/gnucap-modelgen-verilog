
PACKAGE = mgvams

TARGET = \
  mg_func_math.so \
  mg_func_sys.so

NOTYET_TARGET = \
  mg_task.so

MAKE_CPPFLAGS = -I ${SRCDIR}/../src

INSTALL_FILES = $(TARGET)
INSTALL_DIR = $(PREFIX)/lib/gnucap/${PACKAGE}

SRCS = ${TARGET:%.so=%.cc}

CLEANFILES = ${TARGET}
