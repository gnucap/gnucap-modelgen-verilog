
PACKAGE = mgsim

TARGET = \
  c_param.so \
  d_ltra.so \
  d_va_acs.so \
  d_va_filter.so \
  d_va_laplace.so \
  d_va_noise.so \
  d_va_slew.so \
  d_va_zi.so \
  d_vaflow.so \
  d_vapot_br.so \
  d_vapot.so \
  d_vasw.so \
  lang_verilog.so \
  v_instance.so \
  v_paramset.so \
  v_module.so

RAW_SRCS = ${TARGET:%.so=%.cc}
SRCS = ${RAW_SRCS}

INSTALL_FILES = $(TARGET)
INSTALL_DIR = $(PREFIX)/lib/gnucap/${PACKAGE}

MAKE_CPPFLAGS = -I ..

CLEANFILES = ${TARGET}
