


load ../vams/capacitor.so
load ../vams/resistor.so

options debug

options bypass
verilog

module main(in out gnd);
capacitor #(.c(.5) .$mfactor(2)) c1(gnd, out);
resistor #(.r(2) .$mfactor(2)) r1(in, out)
endmodule

main #() dut(in out 0)

spice

V1 in 0 ac 1 dc 0.9 tran sin

.print tran v(nodes) iter(0)
.print dc v(nodes) iter(0)
.print ac vm(nodes) vp(nodes)

.dc
.tran 1
.ac 1 10 * 2
.status notime
