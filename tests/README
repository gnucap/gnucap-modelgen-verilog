== gnucap modelgen tests

Run with "make check", or "./test ../gnucap-verilog '' check ref".

Desired output is stored in ref, actual output will be in check.
See check/.diffs for results (no diff == good).
