

attach ./modelgen_0.so


verilog

`modelgen
module test_strobe0()

	analog begin : main
		real one;
		$strobe("1hello %f", 1.);
		one = 1;
		$write("2hello \
          foo %f\n", one);
		$strobe("time is \\ foo %f", $abstime);
		$strobe("gmin is %g", $simparam("gmin"));
		$strobe("iter is %f, def %f", $simparam("iteration")-1, $simparam("foo", 42.));
	end

endmodule

!make test_strobe0.so > /dev/null
attach ./test_strobe0.so

test_strobe0 #() dut();

list

print dc iter(0)
dc

options gmin 1e-6
print tr iter(0)
tr 1
end
