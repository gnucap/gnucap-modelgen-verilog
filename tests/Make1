
# these are hacks, only used in tests, not installed, ignore.
TEST_PLUGINS = \
  d_reject.so \
  bm_pulse.so \
  modelgen_0.so \
  modelgen_1.so

GNUCAP = gnucap
GNUCAP_ARGS = -a mgsim
MG_ARGS = -a mgvams
MODELGEN = ../src/gnucap-mg-vams

CLEANFILES = ${SYMLINKS} ${TEST_PLUGINS} *.so
