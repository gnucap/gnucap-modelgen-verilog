verilog
load mgsim

// the model we just compiled
load ./rc_b.so

parameter ic=1

// instanciate it.
ground gnd;
rc #(.ic(ic)) myrc1(\1 , \2 , gnd);
resistor #(.r(1k)) r1(\2 , gnd);

print tran v(nodes)
tran 1m
