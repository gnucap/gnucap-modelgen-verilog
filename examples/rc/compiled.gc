verilog
load mgsim
load vams/capacitor.so

// the model we just compiled
load ./rc.so

parameter ic=1

// instanciate it.
ground gnd;
rc #(.ic(ic)) myrc2(\1 , \2 );
resistor #(.r(1k)) r1(\2 , gnd);

print tran v(nodes)
tran 1m
status notime
