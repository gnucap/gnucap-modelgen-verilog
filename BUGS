== modelgen
- preprocessor mangles newlines, need INC_FILE
- V(..) <+ I(..) does not work, see mg2_ccvs.1.gc

== simulator plugins
- paramset: requires newlines, v_paramset.2a.gc
- c_pulse.so: obsolete hack, need discontinuities.

== libgnucap, TODO
- need dollar in identifiers
- need string tokens
- MODEL_CARD in lang interface
